import React from "react";
import ReactDOM from "react-dom/client";
import DataNextApp from "./datanext/DataNextApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DataNextApp />
  </React.StrictMode>
);
